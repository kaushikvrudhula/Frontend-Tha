
const Steps = () =>{
return(
<div className="steps">
    <h1>Steps</h1>
    <ul>
        <li>Create a user state using redux</li>
        <li>Update user state when input is changed</li>
        <li>Create a separate component and display user data(don't pass it as props)</li>
    </ul>
    </div>
);
}
export default Steps;
